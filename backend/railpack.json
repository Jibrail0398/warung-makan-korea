{
  "$schema": "https://schema.railpack.com",
  "provider": "php",
  "steps": {
    "install": {
      "commands": [
        "...",
        "PUPPETEER_SKIP_DOWNLOAD=true PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true npm ci --prefix vendor/kstmostofa/laravel-whatsapp/sidecar"
      ]
    }
  },
  "deploy": {
    "aptPackages": ["ca-certificates", "chromium", "git", "unzip", "zip"]
  }
}
